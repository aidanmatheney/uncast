export const appUrl = `${window.location.protocol}//${window.location.hostname}${window.location.port ? `:${window.location.port}` : ''}`;
export const clientId = 'uncast-webapp';

export const apiUrl = 'https://localhost:5001';
export const apiScope = 'Uncast.WebApiAPI';
